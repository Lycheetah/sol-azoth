# 2026-07-09 — Arc 8 (Grok Build)

## What shipped
- `launch_azoth_full.sh` + bashrc `azoth-full` → real multi-body boot again
- `azoth` / `az` remain single-agent (`launch_sol.sh`)
- Tools: `survey`, `research` (+ existing `glob`)
- `/scout apply|status` + auto-apply after scout → MODEL_HEALTH.md + scout_top_pick.json
- `/plan` writes WORKSPACE/plans/, gates `/forge` until `/approve` (override `/forge !`)
- Unattended Gate-2 Π false-stuck fixed; STUCK_FLAG cleared
- `/help` and `/chat` now actually print (REPL was discarding return strings)
- Keys env-only; no literals in agent.py
- Tests: 31 pass (was 24)

## Mac verify
1. `azoth` → casual chat stays chat
2. `/help` → see printed help
3. `/scout apply` → MODEL_HEALTH ranking
4. `/plan something big` → then `/approve` → `/forge`
5. `azoth-full` only when you want Luna+Scout+auto-forge

## Do not commit with keys
`.env` holds DEEPSEEK/NVIDIA. agent.py fallbacks are `""`.
